import React from 'react';

type LabelProps = {
  children?: React.ReactNode;
  for?: string;
  width?: string;
  height?: string;
  padding?: string;
  background?: string;
  border?: string;
  color?: string;
  fontFamily?: string;
  fontSize?: string;
};

export default LabelProps;
